-- Does the following statement use the DDL or DML component of SQL?

DELETE FROM things WHERE item = 'Cup';

-- We are manipulating the data and not the schema, structure.  We are
-- removing specific rows of data from a table. 
