-- Does the following statement use the DDL or DML component of SQL?

CREATE SEQUENCE part_number_sequence;

-- CREATE keyword is thought of as definition.  Here we are modifying
-- the characteristics and attributes of a database.  We are giving the
-- database a sequence object.
